# Chaos Core Map Export

Runtime entry: `oak_square.fieldmap.json`
Content id: `oak_square`

Importer notes:
- The runtime file already matches Chaos Core's field map shape closely.
- `interactionZones` are exported explicitly for all interactive map content.
- `oak_square.source.json` preserves the original Technica authoring model.
