# Chaos Core Dialogue Export

Runtime entry: `village_guide_intro.dialogue.json`
Content id: `village_guide_intro`

Importer notes:
- The runtime file contains a normalized dialogue node graph with stable node ids.
- Structured effects are exported explicitly; the runtime does not need to re-parse author text.
- `village_guide_intro.source.json` and `village_guide_intro.dialogue.txt` preserve the Technica authoring source.
